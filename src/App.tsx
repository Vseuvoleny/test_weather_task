import React, { FC } from "react";
import GlobalStyle from "./globalStyles";
import { AppContainer } from "./App.styles";

const App: FC<any> = () => {
  return (
    <AppContainer>
      <GlobalStyle />
      Ghbdtn
    </AppContainer>
  );
};

export default App;
